import 'package:flutter/material.dart';

class AllMyAddressesScreen extends StatelessWidget {
  const AllMyAddressesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
